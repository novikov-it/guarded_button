name: guarded_button
description: Сircular progress indicator while processing and repeated click prevention for material buttons for better UX. 
version: 0.1.4
homepage: https://github.com/Disregard-Therest/guarded_button
repository: https://github.com/Disregard-Therest/guarded_button.git

environment:
  sdk: '>=2.18.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.21.4
